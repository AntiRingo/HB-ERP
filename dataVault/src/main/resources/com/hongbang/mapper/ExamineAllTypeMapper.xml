<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hongbang.mapper.ExamineAllTypeMapper">

    <insert id="insert">
        insert into examine_all_type (id, type_id, department_id, level, step,delete_sign) values (null,#{typeId},#{departmentId},#{level},#{step},#{deleteSign})
        <selectKey  keyProperty="id" order="AFTER" resultType="java.lang.Integer" >
            select last_insert_id();
        </selectKey>
    </insert>

    <insert id="add">
        insert into examine_all_type (id, type_id, department_id, level, step,delete_sign) values (null,#{typeId},#{departmentId},#{level},#{step},#{deleteSign})
        <selectKey  keyProperty="id" order="AFTER" resultType="java.lang.Integer" >
            select last_insert_id();
        </selectKey>
    </insert>
</mapper>