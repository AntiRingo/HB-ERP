<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hongbang.mapper.MappingMapper">

<!--    循环添加映射-->
    <insert id="addMapExcel">
        insert into mapping (id,att_name_id, sort_id, begin_location, end_location,length) VALUES
        <foreach collection="mappings" item="mappings" index="index" separator=",">
            (
            null,
            #{mappings.attNameId,jdbcType=INTEGER},
            #{mappings.sortId,jdbcType=INTEGER},
            #{mappings.beginLocation,jdbcType=INTEGER},
            #{mappings.endLocation,jdbcType=INTEGER},
             #{mappings.length,jdbcType=INTEGER}
            )
        </foreach>

    </insert>
<!--    循环更新映射-->
    <update id="updateMapExcel" parameterType="java.util.List">
        <foreach collection="mappings" item="mappings" index="index" separator=";">
            update mapping
                <set>
                    mapping.begin_location=#{mappings.beginLocation,jdbcType=INTEGER},
                    mapping.end_location = #{mappings.endLocation,jdbcType=INTEGER},
                    mapping.length = #{mappings.length,jdbcType=INTEGER}
                </set>
                    where mapping.id = #{mappings.id,jdbcType=INTEGER}
        </foreach>

    </update>
    <delete id="deleteMapExcelNextAdd">
        delete from  mapping where mapping.sort_id in
        <foreach collection="mappings" item="mappings" index="index" separator="," open="(" close=")">
            #{mappings.sortId,jdbcType=INTEGER}
        </foreach>
    </delete>


</mapper>